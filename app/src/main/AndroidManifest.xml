<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.edu.tesoem.isc.leco.proyectoleco">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".acercade"></activity>
        <activity android:name=".opensuse" />
        <activity android:name=".pdf" />
        <activity android:name=".mozilla" />
        <activity android:name=".libre" />
        <activity android:name=".gimp" />
        <activity android:name=".clamav" />
        <activity android:name=".chromium" />
        <activity android:name=".menu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>